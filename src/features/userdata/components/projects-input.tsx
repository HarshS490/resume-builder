"use client";

import { UseFormReturn } from "react-hook-form";

type ProjectsInputProps = {
  form: UseFormReturn;
};

export const ProjectsInput = ({}: ProjectsInputProps) => {
  return <></>
  
};
