"use client";

import { UseFormReturn } from "react-hook-form";

type WorkExperienceInputProps = {
  form: UseFormReturn;
};

export const WorkExperienceInput = ({}: WorkExperienceInputProps) => {
  return <></>
};
