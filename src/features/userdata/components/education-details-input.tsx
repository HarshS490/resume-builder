"use client";

import { UseFormReturn } from "react-hook-form";

type EducationDetailsInputProps = {
  form: UseFormReturn;
};

export const EducationDetailsInput = ({}: EducationDetailsInputProps) => {
  return <></>
};
